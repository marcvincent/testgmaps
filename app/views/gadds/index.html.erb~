
<% provide(:title, 'views/gadds/index') %>


	<%= gmaps4rails(@json) %>
<%= render 'gmaps4rails_new' %>


<br />

<%= render 'form' %>

<%= link_to 'New Gadd', new_gadd_path %>

<br />
<br />

<%= check_box_tag(:showGaddTable) %>
<br />
<br />
<br />
<table>
  <tr>
    <th>Name</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th>User</th>
    <th>Gmaps zoom</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% if :showGaddTable %>
	<% @gadds.each do |gadd| %>
	  <tr>
		 <td><%= gadd.name %></td>
		 <td><%= gadd.latitude %></td>
		 <td><%= gadd.longitude %></td>
		  <td><%= gadd.gmaps_zoom %></td>
		 <td><%= link_to 'Show', gadd %></td>
		 <td><%= link_to 'Edit', edit_gadd_path(gadd) %></td>
		 <td><%= link_to 'Destroy', gadd, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	  </tr>
	  <% end %>
	</table>
<% end %>
